
숫자는 0이 된다.
계속 숫자가 5보다 작다인 동안 다음
    숫자를 출력한다.
    숫자는 숫자와 1을 더한 것이 된다.
문단을 반복한다.


배열은 [10 다음 20 다음 30]이 된다.
배열에 있는 각 항목들을 숫자로 가져와 다음
    숫자를 출력한다.
문단을 반복한다.


사전는 {"이름"은 "홍길동" 또 "나이"는 30}이 된다.
사전에 있는 각 항목들을 키로 가져와 다음
    키와 " "를 더한 것을 줄바꿈없이_출력한다.
    사전에서 키번째 원소를 출력한다.
문단을 반복한다.


계속 참인 동안 다음
    가는 입력한 것을 정수로_변환한 것이 된다.
    만약 가가 1이랑 같다면 다음 나간다. 문단을 실행한다.
    아니고 만약 가가 2랑 같다면 다음 계속한다. 문단을 실행한다.
    가를 출력한다.
문단을 반복한다.