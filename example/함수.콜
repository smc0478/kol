
함수 팩토리얼을_구한다는 가로 다음
    만약 가가 0이랑 같다면 다음
        결과 값은 1이 된다. 그리고 끝난다.
    문단을 실행한다.
    결과 값은 가와 ((가를 1로 뺀 것)으로 팩토리얼을_구한 것)을 곱한 것이 된다. 
    그리고 끝난다.
문단을 실행한다.

4로 팩토리얼을_구한 것을 출력한다.

함수 인사는 이름으로 다음
    이름과 "님 안녕하세요!"를 더한 것을 출력한다.
문단을 실행한다.

"홍길동"으로 인사한다.

함수 바꾼다는 가와 나로 다음
    결과 값은 나가 된다. 그리고 끝난다.
문단을 실행한다.

1을 2로 바꾼 것을 출력한다.


합계는 10과 20을 더한 것이 된다.
합계를 출력한다.