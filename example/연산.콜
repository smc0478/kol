

(1이 2랑 같다는 것)을 출력한다.
(1이 1랑 같다는 것)을 출력한다.
(1이 2랑 다르다는 것)을 출력한다.
(1이 1랑 다르다는 것)을 출력한다.
(1이 "2"랑 비슷한 것)을 출력한다.
(1이 "1"랑 비슷한 것)을 출력한다.
(1이 "1"랑 같다는 것)을 출력한다.

(1이 "2"랑 안 비슷한 것)을 출력한다.
(1이 "1"랑 안 비슷한 것)을 출력한다.

(1이 3보다 크다는 것)을 출력한다.
(3이 1보다 크다는 것)을 출력한다.
(1이 3보다 작다는 것)을 출력한다.
(1이 1보다 작다는 것)을 출력한다.
(1이 1보다 크거나 같다는 것)을 출력한다.

(1이 3만큼 왼쪽으로 간 것)을 출력한다.
(8이 3만큼 오른쪽으로 간 것)을 출력한다.

배열은 0과 4로 범위를_만든 것이 된다.
배열에서 3번째 원소를 출력한다.

사전은 {"가"는 1 또 "나"는 2}가 된다.
사전의 나를 출력한다.

(참 그리고 거짓)을 출력한다.
(참 그리고 참)을 출력한다.
(참 또는 거짓)을 출력한다.
(거짓 또는 거짓)을 출력한다.